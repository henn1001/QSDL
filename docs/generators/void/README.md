# Void (no-op) generator

The `void` generator intentionally produces no output.

Use it when you want to:

- parse `.qsdl` schemas,
- run processors/validation,
- and exercise the core pipeline without generating artifacts.
