/**
 * NOTE: This class was generated by QSDL.
 */
package app.server.common.util;

import jakarta.json.JsonMergePatch;
import jakarta.json.JsonReader;
import jakarta.json.JsonWriter;
import java.io.IOException;
import org.springframework.http.HttpInputMessage;
import org.springframework.http.HttpOutputMessage;
import org.springframework.http.MediaType;
import org.springframework.http.converter.AbstractHttpMessageConverter;
import org.springframework.http.converter.HttpMessageNotReadableException;
import org.springframework.http.converter.HttpMessageNotWritableException;
import org.springframework.stereotype.Component;

/**
 * Spring HTTP message converter for {@link JsonMergePatch}.
 */
@Component
public class JsonMergePatchConverter extends AbstractHttpMessageConverter<JsonMergePatch> {

    public JsonMergePatchConverter() {
        super(MediaType.APPLICATION_JSON, MediaTypeExtension.APPLICATION_MERGE_PATCH);
    }

    @Override
    protected boolean supports(Class<?> clazz) {
        return JsonMergePatch.class.isAssignableFrom(clazz);
    }

    @Override
    protected JsonMergePatch readInternal(Class<? extends JsonMergePatch> clazz, HttpInputMessage inputMessage)
            throws IOException, HttpMessageNotReadableException {
        try (JsonReader reader = jakarta.json.Json.createReader(inputMessage.getBody())) {
            return jakarta.json.Json.createMergePatch(reader.readValue());
        } catch (Exception e) {
            String msg = "Could not read JSON Merge Patch: " + e.getMessage();
            throw new HttpMessageNotReadableException(msg, e, inputMessage);
        }
    }

    @Override
    protected void writeInternal(JsonMergePatch jsonMergePatch, HttpOutputMessage outputMessage)
            throws IOException, HttpMessageNotWritableException {
        try (JsonWriter writer = jakarta.json.Json.createWriter(outputMessage.getBody())) {
            writer.write(jsonMergePatch.toJsonValue());
        } catch (Exception e) {
            String msg = "Could not write JSON Merge Patch: " + e.getMessage();
            throw new HttpMessageNotWritableException(msg, e);
        }
    }

    public static class MediaTypeExtension extends MediaType {
        public static final MediaType APPLICATION_MERGE_PATCH = MediaType.valueOf("application/merge-patch+json");

        public MediaTypeExtension(String type) {
            super(type);
        }
    }
}
