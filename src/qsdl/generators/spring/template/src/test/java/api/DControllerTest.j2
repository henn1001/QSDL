{% import '_macro/Parameter.j2' as macro with context %}
{% import '_macro/Util.j2' as util with context %}
/**
 * NOTE: This class was generated by QSDL.
 */
package {{api.package.controller}};

{{ util.macro_imports(self._TemplateReference__context.name, model) }}

@WebMvcTest({{model.name}}Controller.class)
@Import(TestConfig.class)
class {{model.name}}ControllerTest {

    @MockitoBean
    {{model.name}}Service service;

    @Value("{{basePath}}")
    String basePath;

    @Autowired
    MockMvc mockMvc;

    static {{id_type}} one = {{ "1L" if id_type == "Long" else '"1"' }};
{% for operation in api.operations if operation.is_generated %}

    {% if operation.method == "get" and operation.is_pageable %}
    {# 
    ########################################################################################
    #}
    @Test
    public void when{{operation.name|pascal}}_thenOk() throws Exception {

        // Given
        {{model.name}} responseDto = TestUtils.getRandom({{model.name}}.class);

        CursorPage<{{model.name}}> ret = new CursorPage<{{model.name}}>(Arrays.asList(responseDto), null, null);

        when(service.{{operation.name}}({{macro.paramsCallServiceMocked(operation)}}))
                .thenReturn(ret);

        // When
        String response = mockMvc.perform(get(basePath + "{{ operation.path | regex_replace("\{[a-zA-Z0-9_-]+\}", "1") }}"))
                .andExpect(status().isOk())
                .andReturn()
                .getResponse()
                .getContentAsString();

        // Then
        JSONAssert.assertEquals(
                JsonUtil.toString(ret),
                new JSONObject(response),
                false);
    }
    {% elif operation.method == "post" and ( operation.path.endswith('/add') or operation.path.endswith('/remove') ) %}
    {# 
    ########################################################################################
    #}
    @Test
    public void when{{operation.name|pascal}}_thenOk() throws Exception {

        // Given

        // When
        mockMvc.perform(post(basePath + "{{ operation.path | regex_replace("\{[a-zA-Z0-9_-]+\}", "1") }}"))
                .andExpect(status().isOk());

        // Then

    }
    {% elif operation.method == "post" %}
    {# 
    ########################################################################################
    #}
    @Test
    public void when{{operation.name|pascal}}_thenOk() throws Exception {

        // Given
        {{model.name}}Request requestDto = TestUtils.getRandom({{model.name}}Request.class);
        {{model.name}} responseDto = TestUtils.getRandom({{model.name}}.class);

        when(service.{{operation.name}}({{macro.paramsCallServiceMocked(operation)}}))
                .thenReturn(responseDto);

        // When
        String response = mockMvc.perform(post(basePath + "{{ operation.path | regex_replace("\{[a-zA-Z0-9_-]+\}", "1") }}")
                        .content(JsonUtil.toString(requestDto))
                        .contentType(MediaType.APPLICATION_JSON))
                .andExpect(status().isOk())
                .andReturn()
                .getResponse()
                .getContentAsString();

        // Then
        JSONAssert.assertEquals(
                JsonUtil.toString(responseDto),
                new JSONObject(response),
                false);
    }

    @Test
    public void when{{operation.name|pascal}}WithInvalidPayload_thenError() throws Exception {

        // Given
        {{model.name}} responseDto = TestUtils.getRandom({{model.name}}.class);

        when(service.{{operation.name}}({{macro.paramsCallServiceMocked(operation)}}))
                .thenReturn(responseDto);

        // When
        String response = mockMvc.perform(post(basePath + "{{ operation.path | regex_replace("\{[a-zA-Z0-9_-]+\}", "1") }}")
                .content("{{ "{}" if model.has_required else "" }}")
                .contentType(MediaType.APPLICATION_JSON))
                .andExpect(status().isBadRequest())
                .andReturn()
                .getResponse()
                .getContentAsString();

        // Then
        AppError error = JsonUtil.fromJson(response, AppError.class);
        assertEquals(ErrorCode.BAD_REQUEST.code(), error.code());
        assertEquals(ErrorCode.BAD_REQUEST.message(), error.message());
        assertEquals(ErrorCode.BAD_REQUEST.status(), error.status());
    }
    {% elif operation.method == "get" %}
    {# 
    ########################################################################################
    #}
    @Test
    public void when{{operation.name|pascal}}_thenOk() throws Exception {

        // Given
        {{model.name}} responseDto = TestUtils.getRandom({{model.name}}.class);

        when(service.{{operation.name}}({{ "eq(one), eq(one)" if operation.parent else "eq(one)" }}))
                .thenReturn(responseDto);

        // When
        String response = mockMvc.perform(get(basePath + "{{ operation.path | regex_replace("\{[a-zA-Z0-9_-]+\}", "1") }}"))
                .andExpect(status().isOk())
                .andReturn()
                .getResponse()
                .getContentAsString();

        // Then
        JSONAssert.assertEquals(
                JsonUtil.toString(responseDto),
                new JSONObject(response),
                false);
    }
    {% elif operation.method == "patch" %}
    {# 
    ########################################################################################
    #}
    @Test
    public void when{{operation.name|pascal}}_thenOk() throws Exception {

        // Given
        {{model.name}}Request requestDto = TestUtils.getRandom({{model.name}}Request.class);
        {{model.name}} responseDto = TestUtils.getRandom({{model.name}}.class);

        when(service.{{operation.name | replace("update", "get")}}({{ "eq(one), eq(one)" if operation.parent else "eq(one)" }}))
                .thenReturn(responseDto);

        when(service.{{operation.name}}({{ "eq(one), eq(one), any()" if operation.parent else "eq(one), any()" }}))
                .thenReturn(responseDto);

        // When
        String response = mockMvc.perform(patch(basePath + "{{ operation.path | regex_replace("\{[a-zA-Z0-9_-]+\}", "1") }}")
                        .content(JsonUtil.toString(requestDto))
                        .contentType(MediaTypeExtension.APPLICATION_MERGE_PATCH))
                .andExpect(status().isOk())
                .andReturn()
                .getResponse()
                .getContentAsString();

        // Then
        JSONAssert.assertEquals(
                JsonUtil.toString(responseDto),
                new JSONObject(response),
                false);
    }

    @Test
    public void when{{operation.name|pascal}}WithInvalidPayload_thenError() throws Exception {

        // Given
        {{model.name}} responseDto = TestUtils.getRandom({{model.name}}.class);

        when(service.{{operation.name}}({{ "eq(one), eq(one), any()" if operation.parent else "eq(one), any()" }}))
                .thenReturn(responseDto);

        // When
        String response = mockMvc.perform(patch(basePath + "{{ operation.path | regex_replace("\{[a-zA-Z0-9_-]+\}", "1") }}")
                .content("{{ "{}" if model.has_required else "" }}")
                .contentType(MediaTypeExtension.APPLICATION_MERGE_PATCH))
                .andExpect(status().isBadRequest())
                .andReturn()
                .getResponse()
                .getContentAsString();

        // Then
        AppError error = JsonUtil.fromJson(response, AppError.class);
        assertEquals(ErrorCode.BAD_REQUEST.code(), error.code());
        assertEquals(ErrorCode.BAD_REQUEST.message(), error.message());
        assertEquals(ErrorCode.BAD_REQUEST.status(), error.status());
    }
    {% elif operation.method == "delete" %}
    {# 
    ########################################################################################
    #}
    @Test
    public void when{{operation.name|pascal}}_thenOk() throws Exception {

        // Given

        // When
        mockMvc.perform(delete(basePath + "{{ operation.path | regex_replace("\{[a-zA-Z0-9_-]+\}", "1") }}"))
                .andExpect(status().isOk());

        // Then

    }
    {% endif %}
{% endfor %}}
{{""}}