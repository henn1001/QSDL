/**
 * NOTE: This class was generated by QSDL.
 */
package {{base_package}}.repository;

import static org.junit.jupiter.api.Assertions.assertEquals;
import java.util.*;
import java.util.stream.Collectors;
import com.fasterxml.jackson.databind.node.ObjectNode;
import org.jeasy.random.EasyRandom;
import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.autoconfigure.orm.jpa.DataJpaTest;
import org.springframework.context.annotation.Import;

import {{base_package}}.domain.*;
import {{base_package}}.model.AppPageable;
import {{base_package}}.util.Json;

@DataJpaTest
@Import({{base_package}}.TestConfig.class)
public class {{model.name}}RepositoryTest {

  @Autowired
  private EasyRandom easyRandom;

{% for parent in model.parents %}
  @Autowired
  private {{parent.name}}Repository {{parent.name|lower}}Repository;

{% endfor %}
  @Autowired
  private {{model.name}}Repository {{model.name|lower}}Repository;

  @Test
  public void whenSave_thenFind() {

    // Given
    {{model.name}} testData = easyRandom.nextObject({{model.name}}.class);
    testData.removeRelations();

    // When
    {{model.name}} dbData = {{model.name|lower}}Repository.saveAndFlush(testData);
    {{model.name}} findData = {{model.name|lower}}Repository.findById(dbData.getId()).orElse(null);
    testData.copyIdentiy(findData);

    // Then
    ObjectNode node1 = Json.serializer().nodeFromObject(testData);
    ObjectNode node2 = Json.serializer().nodeFromObject(findData);
    assertEquals(node1, node2);
  }

  @Test
  public void whenDelete_thenCountZero() {

    // Given
    {{model.name}} testData = easyRandom.nextObject({{model.name}}.class);
    testData.removeRelations();
    {{model.name}} dbData = {{model.name|lower}}Repository.saveAndFlush(testData);

    // When
    {{model.name|lower}}Repository.delete(dbData);

    // Then
    long count = {{model.name|lower}}Repository.count();
    assertEquals(0, count);
  }

  @Test
  public void whenCount_thenUseQuerie() {

    // Given
    List<{{model.name}}> testData = easyRandom.objects({{model.name}}.class, 5).collect(Collectors.toList());
    testData.forEach(x -> x.removeRelations());
    List<{{model.name}}> dbData = {{model.name|lower}}Repository.saveAllAndFlush(testData);

    AppPageable pageable = new AppPageable(null, null, null);
    {% for field in model.fields if field._ref.is_query %}
    pageable.query.put("{{field.name}}", dbData.get(0).{{ "get" + field.name|pascal + "()" if encapsulation else field.name}});
    {% endfor %}

    // When
    long count = {{model.name|lower}}Repository.count(pageable);

    // Then
    assertEquals({{ "1" if util.has(model._ref, has_query=True) else "5" }}, count);
  }

  @Test
  public void whenFindAll_thenPaginate() {

    // Given
    List<{{model.name}}> testData = easyRandom.objects({{model.name}}.class, 5).collect(Collectors.toList());
    testData.forEach(x -> x.removeRelations());
    {{model.name|lower}}Repository.saveAllAndFlush(testData);

    // When
    String cursor = null;
    int idx = 0;

    do {
      AppPageable pageable = new AppPageable(cursor, 1l, null);
      List<{{model.name}}> findData = {{model.name|lower}}Repository.findAll(pageable);

      cursor = pageable.getNextCursor(findData);

      assertEquals(1, findData.size());

      idx++;
    } while (cursor != null);

    // Then
    assertEquals(5, idx);
  }

  {% for parent in model.parents %}
  @Test
  public void whenCountBy{{parent.name}}_thenUseQuerie() {

    // Given
    {{parent.name}} tmp = easyRandom.nextObject({{parent.name}}.class);
    tmp.removeRelations();
    {{parent.name}} testParent = {{parent.name|lower}}Repository.saveAndFlush(tmp);

    List<{{model.name}}> testData = easyRandom.objects({{model.name}}.class, 5).collect(Collectors.toList());
    {% for field in model.fields if field.is_relation %}
      {% if encapsulation %}
    testData.forEach(x -> x.set{{field.name|pascal}}(null));
      {% else %}
    testData.forEach(x -> x.{{field.name}} = null);
      {% endif %}
    {% endfor %}
    testData = {{model.name|lower}}Repository.saveAllAndFlush(testData);

    {% if util.is_aggregation(model._ref, parent._ref) %}
      {% for field in parent.fields if field.is_relation and field.type == model._ref.name %}
        {% if encapsulation %}
    testParent.set{{field.name|pascal}}(new HashSet<>(testData));
        {% else %}
    testParent.{{field.name}} = new HashSet<>(testData);
        {% endif %}
    {{parent.name|lower}}Repository.saveAndFlush(testParent);
      {% endfor %}
    {% else %}
      {% for field in model.fields if field.is_relation and field.type == parent._ref.name %}
      {% if encapsulation %}
    testData.forEach(x -> x.set{{field.name|pascal}}(testParent));
      {% else %}
    testData.forEach(x -> x.{{field.name}} = testParent);
      {% endif %}
    testData = {{model.name|lower}}Repository.saveAllAndFlush(testData);
      {% endfor %}
    {% endif %}

    AppPageable pageable = new AppPageable(null, null, null);

    // When
    long count = {{model.name|lower}}Repository.countBy{{ util.get_parent_id_for_repo(parent._ref) | pascal }}(testParent.getId(), pageable);

    // Then
    assertEquals({{ "1" if util.has(model._ref, has_query=True) else "5" }}, count);
  }

  @Test
  public void whenFindAllBy{{parent.name}}_thenPaginate() {

    // Given
    {{parent.name}} tmp = easyRandom.nextObject({{parent.name}}.class);
    tmp.removeRelations();
    {{parent.name}} testParent = {{parent.name|lower}}Repository.saveAndFlush(tmp);

    List<{{model.name}}> testData = easyRandom.objects({{model.name}}.class, 5).collect(Collectors.toList());
    {% for field in model.fields if field.is_relation %}
      {% if encapsulation %}
    testData.forEach(x -> x.set{{field.name|pascal}}(null));
      {% else %}
    testData.forEach(x -> x.{{field.name}} = null);
      {% endif %}
    {% endfor %}
    testData = {{model.name|lower}}Repository.saveAllAndFlush(testData);

    {% if util.is_aggregation(model._ref, parent._ref) %}
      {% for field in parent.fields if field.is_relation and field.type == model._ref.name %}
        {% if encapsulation %}
    testParent.set{{field.name|pascal}}(new HashSet<>(testData));
        {% else %}
    testParent.{{field.name}} = new HashSet<>(testData);
        {% endif %}
    {{parent.name|lower}}Repository.saveAndFlush(testParent);
      {% endfor %}
    {% else %}
      {% for field in model.fields if field.is_relation and field.type == parent._ref.name %}
      {% if encapsulation %}
    testData.forEach(x -> x.set{{field.name|pascal}}(testParent));
      {% else %}
    testData.forEach(x -> x.{{field.name}} = testParent);
      {% endif %}
    testData = {{model.name|lower}}Repository.saveAllAndFlush(testData);
      {% endfor %}
    {% endif %}

    // When
    String cursor = null;
    int idx = 0;

    do {
      AppPageable pageable = new AppPageable(cursor, 1l, null);
      List<{{model.name}}> findData = {{model.name|lower}}Repository.findAllBy{{ util.get_parent_id_for_repo(parent._ref) | pascal }}(testParent.getId(), pageable);

      cursor = pageable.getNextCursor(findData);

      assertEquals(1, findData.size());

      idx++;
    } while (cursor != null);

    // Then
    assertEquals(5, idx);
  }

  {% endfor %}
  {% if model.has_aggregation %}
    {% for parent in model.parents if util.is_aggregation(model._ref, parent._ref) %}
  @Test
  public void whenRemoveRelation_thenLinkRemovedFrom{{parent.name}}() {

    // Given
    {{parent.name}} tmp = easyRandom.nextObject({{parent.name}}.class);
    tmp.removeRelations();
    {{parent.name}} testParent = {{parent.name|lower}}Repository.saveAndFlush(tmp);

    List<{{model.name}}> testData = easyRandom.objects({{model.name}}.class, 5).collect(Collectors.toList());
    {% for field in model.fields if field.is_relation %}
      {% if encapsulation %}
    testData.forEach(x -> x.set{{field.name|pascal}}(null));
      {% else %}
    testData.forEach(x -> x.{{field.name}} = null);
      {% endif %}
    {% endfor %}
    testData = {{model.name|lower}}Repository.saveAllAndFlush(testData);

    {% for field in parent.fields if field.is_relation and field.type == model._ref.name %}
      {% if encapsulation %}
    testParent.set{{field.name|pascal}}(new HashSet<>(testData));
      {% else %}
    testParent.{{field.name}} = new HashSet<>(testData);
      {% endif %}
    {{parent.name|lower}}Repository.saveAndFlush(testParent);
    {% endfor %}

    // When
    {{model.name|lower}}Repository.removeRelations(testData.get(0).getId());

    // Then
    AppPageable pageable = new AppPageable(null, null, null);
    List<{{model.name}}> findData = {{model.name|lower}}Repository.findAllBy{{ util.get_parent_id_for_repo(parent._ref) | pascal }}(testParent.getId(), pageable);

    assertEquals(4, findData.size());
  }

    {% endfor %}
  {% endif %}
}
{{""}}