{% import 'Macro.j2' as macro with context %}
/**
 * NOTE: This class was generated by QSDL.
 */
package {{base_package}}.api.{{api.tag}}.{{api.name}};

import java.util.List;
import java.util.Optional;
import javax.validation.Valid;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.http.HttpStatus;
import org.springframework.http.ResponseEntity;
import org.springframework.stereotype.Controller;
import org.springframework.web.context.request.NativeWebRequest;

import {{base_package}}.model.*;

@Controller
public class {{api.capital_name}}ApiController implements {{api.capital_name}} {

  private static Logger log = LoggerFactory.getLogger({{api.capital_name}}ApiController.class.getSimpleName());

  private final NativeWebRequest request;

  @org.springframework.beans.factory.annotation.Autowired
  public {{api.capital_name}}ApiController(NativeWebRequest request) {
    this.request = request;
  }

  @Override
  public Optional<NativeWebRequest> getRequest() {
    return Optional.ofNullable(request);
  }

{% for operation in api.operations %}
  @Override
  public ResponseEntity<{{macro.get_response(operation)}}> {{operation.name}}({{macro.get_paramsController(operation)}}) throws Exception {
      //TODO: implement me
      return new ResponseEntity<>(HttpStatus.NOT_IMPLEMENTED);
  }

{% endfor %}
}
{{""}}