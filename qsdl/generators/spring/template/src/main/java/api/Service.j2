{% import 'macro/Parameter.j2' as macro with context %}
{% import 'macro/Operation.j2' as opr_macro with context %}
/**
 * NOTE: This class was generated by QSDL.
 */
package {{base_package}}.service;

import java.util.Arrays;
import java.util.List;
import java.util.Map;
import javax.annotation.PostConstruct;
import javax.persistence.EntityNotFoundException;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.stereotype.Service;

import {{base_package}}.constant.AppError;
import {{base_package}}.exception.ApiException;
{% if database == "hibernate" and api.is_crud %}
import {{base_package}}.repository.*;
{% endif %}
import {{base_package}}.model.*;

@Service
public class {{api.name}}Service {

  private static Logger log = LoggerFactory.getLogger({{api.name}}Service.class.getSimpleName());

{% if database == "hibernate" and api.is_crud %}
  @javax.annotation.Resource
  private {{api.name}}Repository {{api.name|lower}}Repository;
{% endif %}

  @PostConstruct
  private void init() {

  }

{% for operation in api.operations %}
  public {{macro.get_response(operation)}} {{operation.name}}({{macro.paramsService(operation)}}) throws Exception {

  {% if database == "hibernate" and api.is_crud %}
  {{- opr_macro.logic(operation) -}}
  {% else %}
  return null;
  {% endif %}
  }

{% endfor %}
}
{{""}}