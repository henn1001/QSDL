{% import 'macro/Parameter.j2' as macro with context %}
{% import 'macro/Operation.j2' as opr_macro with context %}
/**
 * NOTE: This class was generated by QSDL.
 */
package {{base_package}}.api.{{api.tag}}.{{api.name}};

import java.util.Arrays;
import java.util.List;
import java.util.Map;
import javax.annotation.PostConstruct;
import javax.persistence.EntityNotFoundException;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.stereotype.Service;

import {{base_package}}.constants.AppError;
import {{base_package}}.exception.ApiException;
import {{base_package}}.repository.*;
import {{base_package}}.model.*;

@Service
public class {{api.capital_name}}Service {

  private static Logger log = LoggerFactory.getLogger({{api.capital_name}}Service.class.getSimpleName());

{% if database == "hibernate" and api.has_crud %}
  @javax.annotation.Resource
  private {{api.capital_name|singularize}}Repository {{api.name|singularize}}Repository;
{% endif %}

  @PostConstruct
  private void init() {

  }

{% for operation in api.operations %}
  public {{macro.get_response(operation)}} {{operation.name}}({{macro.paramsService(operation)}}) throws Exception {

  {% if database == "hibernate" and api.has_crud %}
  {{- opr_macro.logic(operation) -}}
  {% else %}
  return null;
  {% endif %}
  }

{% endfor %}
}
{{""}}