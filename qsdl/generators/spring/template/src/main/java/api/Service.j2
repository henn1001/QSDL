{% import 'Macro.j2' as macro with context %}
/**
 * NOTE: This class was generated by QSDL.
 */
package {{basePackage}}.api.{{api.tag}}.{{api.name}};

import java.util.Arrays;
import java.util.List;
import java.util.Map;
import javax.annotation.PostConstruct;
import org.jeasy.random.EasyRandom;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.stereotype.Service;

import {{basePackage}}.model.*;

@Service
public class {{api.capital_name}}Service {

  private static Logger log = LoggerFactory.getLogger({{api.capital_name}}Service.class.getSimpleName());

  @PostConstruct
  private void init() {

  }

{% for operation in api.operations %}
  public {{macro.get_response(operation)}} {{operation.name}}({{macro.paramsService(operation)}}) throws Exception {

    // TODO: implement me

  {% if operation.response %}
    EasyRandom easyRandom = new EasyRandom();
    {{operation.response.type}} ret = easyRandom.nextObject({{operation.response.type}}.class);

    {% if operation.response.is_array %}
    return Arrays.asList(ret);
    {% else %}
    return ret;
    {% endif %}
  {% else %}
    return null;
  {% endif %}
  }

{% endfor %}
}
{{""}}