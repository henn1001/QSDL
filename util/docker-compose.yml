version: "3"
services:
  swagger-ui:
    image: swaggerapi/swagger-ui:v3.23.7
    ports:
      - "8080:8080"
    environment:
      SWAGGER_JSON: "/tmp/swagger.yml"
    volumes:
      - ./srcgen/input.yml:/tmp/swagger.yml