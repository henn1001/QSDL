/**
 * NOTE: This class was generated by QSDL.
 */
package com.test.api.incident.submit;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.http.HttpStatus;
import org.springframework.http.ResponseEntity;
import org.springframework.stereotype.Controller;
import org.springframework.validation.annotation.Validated;
import org.springframework.web.bind.annotation.*;
import org.springframework.web.context.request.NativeWebRequest;

import java.util.List;
import java.util.Map;
import java.util.Optional;
import javax.validation.Valid;
import javax.validation.constraints.*;

import com.test.model.*;


@Validated
@RequestMapping("${server.base-path:/}")
@Controller
public class SubmitController {

  private final NativeWebRequest request;

  @Autowired
  SubmitService submitService;

  @Autowired
  public SubmitController(NativeWebRequest request) {
    this.request = request;
  }

  public Optional<NativeWebRequest> getRequest() {
    return Optional.empty();
  }

  /**
   * POST /buzzwords/{buzzword_id}/query/submit : submitQuery
   *
   * @param buzzwordId (required)
   * @param body
   * @return OK (status code 200)
   *         or Unexpected error (status code default)
   */
  @PostMapping(
    value = "/buzzwords/{buzzword_id}/query/submit",
    produces = { "application/json" },
    consumes = { "application/json" }
  )
  public ResponseEntity<QueryMachine> submitQuery(@PathVariable("buzzword_id") Long buzzwordId, @Valid @RequestBody QueryMachine body) throws Exception {
    QueryMachine response = submitService.submitQuery(buzzwordId, body);
    return new ResponseEntity<>(response, HttpStatus.OK);
  }

}
