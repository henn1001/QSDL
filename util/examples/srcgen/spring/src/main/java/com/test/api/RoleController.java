/**
 * NOTE: This class was generated by QSDL.
 */
package com.test.api;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.http.HttpStatus;
import org.springframework.http.ResponseEntity;
import org.springframework.stereotype.Controller;
import org.springframework.validation.annotation.Validated;
import org.springframework.web.bind.annotation.*;
import org.springframework.web.context.request.NativeWebRequest;

import java.util.List;
import java.util.Map;
import java.util.Optional;
import javax.validation.Valid;
import javax.validation.constraints.*;

import com.test.model.*;
import com.test.service.*;
import com.test.util.Validator;


@Validated
@RequestMapping("${server.base-path:/}")
@Controller
public class RoleController {

  private final NativeWebRequest request;

  @Autowired
  RoleService RoleService;

  @Autowired
  public RoleController(NativeWebRequest request) {
    this.request = request;
  }

  public Optional<NativeWebRequest> getRequest() {
    return Optional.empty();
  }

  /**
   * GET /projects/{project_id}/roles : List Roles
   *
   * @param projectId (required)
   * @return OK (status code 200)
   *         or Unexpected error (status code default)
   */
  @GetMapping(
    value = "/projects/{project_id}/roles",
    produces = { "application/json" }
  )
  public ResponseEntity<RoleList> getRoles(@PathVariable("project_id") Long projectId, ApiPageable pageable) throws Exception {
    RoleList response = RoleService.getRoles(projectId, pageable);
    return new ResponseEntity<>(response, HttpStatus.OK);
  }

  /**
   * POST /projects/{project_id}/roles : Create a Role
   *
   * @param projectId (required)
   * @param body
   * @return OK (status code 200)
   *         or Unexpected error (status code default)
   */
  @PostMapping(
    value = "/projects/{project_id}/roles",
    produces = { "application/json" },
    consumes = { "application/json" }
  )
  public ResponseEntity<Role> createRole(@PathVariable("project_id") Long projectId, @Valid @RequestBody Role body) throws Exception {
    Role response = RoleService.createRole(projectId, body);
    return new ResponseEntity<>(response, HttpStatus.OK);
  }

}
