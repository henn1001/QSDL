/**
 * NOTE: This class was generated by QSDL.
 */
package com.test.api._default.submits;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.http.HttpStatus;
import org.springframework.http.ResponseEntity;
import org.springframework.stereotype.Controller;
import org.springframework.validation.annotation.Validated;
import org.springframework.web.bind.annotation.*;
import org.springframework.web.context.request.NativeWebRequest;

import java.util.List;
import java.util.Map;
import java.util.Optional;
import javax.validation.Valid;
import javax.validation.constraints.*;

import com.test.model.*;


@Validated
@RequestMapping("${server.base-path:/}")
@Controller
public class SubmitsController {

  private final NativeWebRequest request;

  @Autowired
  SubmitsService submitsService;

  @Autowired
  public SubmitsController(NativeWebRequest request) {
    this.request = request;
  }

  public Optional<NativeWebRequest> getRequest() {
    return Optional.empty();
  }

  /**
   * POST /query/submits : submitQury
   *
   * @param body
   * @return OK (status code 200)
   *         or Unexpected error (status code default)
   */
  @PostMapping(
    value = "/query/submits",
    produces = { "application/json" },
    consumes = { "application/json" }
  )
  public ResponseEntity<QueryMachine> submitQury(@Valid @RequestBody QueryMachine body) throws Exception {
    QueryMachine response = submitsService.submitQury(body);
    return new ResponseEntity<>(response, HttpStatus.OK);
  }

}
