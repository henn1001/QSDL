/**
 * NOTE: This class was generated by QSDL.
 */
package com.test.api;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.http.HttpStatus;
import org.springframework.http.ResponseEntity;
import org.springframework.stereotype.Controller;
import org.springframework.validation.annotation.Validated;
import org.springframework.web.bind.annotation.*;
import org.springframework.web.context.request.NativeWebRequest;

import java.util.List;
import java.util.Map;
import java.util.Optional;
import javax.validation.Valid;
import javax.validation.constraints.*;

import com.test.model.*;
import com.test.service.*;
import com.test.util.Validator;


@Validated
@RequestMapping("${server.base-path:/}")
@Controller
public class QueryMachineController {

  private final NativeWebRequest request;

  @Autowired
  QueryMachineService QueryMachineService;

  @Autowired
  public QueryMachineController(NativeWebRequest request) {
    this.request = request;
  }

  public Optional<NativeWebRequest> getRequest() {
    return Optional.empty();
  }

  /**
   * POST /query/submit : submitQuery
   *
   * @param body
   * @return OK (status code 200)
   *         or Unexpected error (status code default)
   */
  @PostMapping(
    value = "/query/submit",
    produces = { "application/json" },
    consumes = { "application/json" }
  )
  public ResponseEntity<QueryMachine> submitQuery(@Valid @RequestBody QueryMachine body) throws Exception {
    QueryMachine response = QueryMachineService.submitQuery(body);
    return new ResponseEntity<>(response, HttpStatus.OK);
  }

}
