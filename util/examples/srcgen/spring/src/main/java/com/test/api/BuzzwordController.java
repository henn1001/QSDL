/**
 * NOTE: This class was generated by QSDL.
 */
package com.test.api;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.http.HttpStatus;
import org.springframework.http.ResponseEntity;
import org.springframework.stereotype.Controller;
import org.springframework.validation.annotation.Validated;
import org.springframework.web.bind.annotation.*;
import org.springframework.web.context.request.NativeWebRequest;

import java.util.List;
import java.util.Map;
import java.util.Optional;
import javax.validation.Valid;
import javax.validation.constraints.*;

import com.test.model.*;
import com.test.service.*;
import com.test.util.Validator;


@Validated
@RequestMapping("${server.base-path:/}")
@Controller
public class BuzzwordController {

  private final NativeWebRequest request;

  @Autowired
  BuzzwordService BuzzwordService;

  @Autowired
  public BuzzwordController(NativeWebRequest request) {
    this.request = request;
  }

  public Optional<NativeWebRequest> getRequest() {
    return Optional.empty();
  }

  /**
   * GET /buzzwords : getBuzzwords
   *
   * @param name
   * @return OK (status code 200)
   *         or Unexpected error (status code default)
   */
  @GetMapping(
    value = "/buzzwords",
    produces = { "application/json" }
  )
  public ResponseEntity<List<Buzzword>> getBuzzwords(@Valid @RequestParam(value = "name", required = false) String name) throws Exception {
    List<Buzzword> response = BuzzwordService.getBuzzwords(name);
    return new ResponseEntity<>(response, HttpStatus.OK);
  }

  /**
   * POST /buzzwords : createBuzzword
   *
   * @param body
   * @return OK (status code 200)
   *         or Unexpected error (status code default)
   */
  @PostMapping(
    value = "/buzzwords",
    produces = { "application/json" },
    consumes = { "application/json" }
  )
  public ResponseEntity<Buzzword> createBuzzword(@Valid @RequestBody Buzzword body) throws Exception {
    Buzzword response = BuzzwordService.createBuzzword(body);
    return new ResponseEntity<>(response, HttpStatus.OK);
  }

  /**
   * GET /buzzwords/{id} : getBuzzword
   *
   * @param id (required)
   * @return OK (status code 200)
   *         or Unexpected error (status code default)
   */
  @GetMapping(
    value = "/buzzwords/{id}",
    produces = { "application/json" }
  )
  public ResponseEntity<Buzzword> getBuzzword(@PathVariable("id") Long id) throws Exception {
    Buzzword response = BuzzwordService.getBuzzword(id);
    return new ResponseEntity<>(response, HttpStatus.OK);
  }

  /**
   * PUT /buzzwords/{id} : updateBuzzword
   *
   * @param id (required)
   * @param body
   * @param test
   * @return OK (status code 200)
   *         or Unexpected error (status code default)
   */
  @PutMapping(
    value = "/buzzwords/{id}",
    produces = { "application/json" },
    consumes = { "application/json" }
  )
  public ResponseEntity<Buzzword> updateBuzzword(@PathVariable("id") Long id, @RequestBody Map<String, Object> body) throws Exception {
    Buzzword response = BuzzwordService.updateBuzzword(id, body);
    return new ResponseEntity<>(response, HttpStatus.OK);
  }

  /**
   * DELETE /buzzwords/{id} : deleteBuzzword
   *
   * @param id (required)
   * @return OK (status code 200)
   *         or Unexpected error (status code default)
   */
  @DeleteMapping(
    value = "/buzzwords/{id}"
  )
  public ResponseEntity<Void> deleteBuzzword(@PathVariable("id") Long id) throws Exception {
    BuzzwordService.deleteBuzzword(id);
    return new ResponseEntity<>(HttpStatus.OK);
  }

}
