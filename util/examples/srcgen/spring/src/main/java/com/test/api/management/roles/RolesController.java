/**
 * NOTE: This class was generated by QSDL.
 */
package com.test.api.management.roles;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.http.HttpStatus;
import org.springframework.http.ResponseEntity;
import org.springframework.stereotype.Controller;
import org.springframework.validation.annotation.Validated;
import org.springframework.web.bind.annotation.*;
import org.springframework.web.context.request.NativeWebRequest;

import java.util.List;
import java.util.Map;
import java.util.Optional;
import javax.validation.Valid;
import javax.validation.constraints.*;

import com.test.model.*;


@Validated
@RequestMapping("${server.base-path:/}")
@Controller
public class RolesController {

  private final NativeWebRequest request;

  @Autowired
  RolesService rolesService;

  @Autowired
  public RolesController(NativeWebRequest request) {
    this.request = request;
  }

  public Optional<NativeWebRequest> getRequest() {
    return Optional.empty();
  }

  /**
   * GET /projects/{project_id}/roles : List Roles
   *
   * @param projectId (required)
   * @return OK (status code 200)
   *         or Unexpected error (status code default)
   */
  @GetMapping(
    value = "/projects/{project_id}/roles",
    produces = { "application/json" }
  )
  public ResponseEntity<RoleList> getRolesForProject(@PathVariable("project_id") Long projectId, ApiPageable pageable) throws Exception {
    RoleList response = rolesService.getRolesForProject(projectId, pageable);
    return new ResponseEntity<>(response, HttpStatus.OK);
  }

  /**
   * POST /projects/{project_id}/roles : Create a Role
   *
   * @param projectId (required)
   * @param body (required)
   * @return OK (status code 200)
   *         or Unexpected error (status code default)
   */
  @PostMapping(
    value = "/projects/{project_id}/roles",
    produces = { "application/json" },
    consumes = { "application/json" }
  )
  public ResponseEntity<Role> createRoleForProject(@PathVariable("project_id") Long projectId, @Valid @RequestBody Role body) throws Exception {
    Role response = rolesService.createRoleForProject(projectId, body);
    return new ResponseEntity<>(response, HttpStatus.OK);
  }

  /**
   * GET /projects/{project_id}/roles/{role} : Read the specified Role
   *
   * @param projectId (required)
   * @param role (required)
   * @return OK (status code 200)
   *         or Unexpected error (status code default)
   */
  @GetMapping(
    value = "/projects/{project_id}/roles/{role}",
    produces = { "application/json" }
  )
  public ResponseEntity<Role> getRoleForProject(@PathVariable("project_id") Long projectId, @PathVariable("role") Long role) throws Exception {
    Role response = rolesService.getRoleForProject(projectId, role);
    return new ResponseEntity<>(response, HttpStatus.OK);
  }

  /**
   * PUT /projects/{project_id}/roles/{role} : Replace the specified Role
   *
   * @param projectId (required)
   * @param role (required)
   * @param body (required)
   * @return OK (status code 200)
   *         or Unexpected error (status code default)
   */
  @PutMapping(
    value = "/projects/{project_id}/roles/{role}",
    produces = { "application/json" },
    consumes = { "application/json" }
  )
  public ResponseEntity<Role> replaceRoleForProject(@PathVariable("project_id") Long projectId, @PathVariable("role") Long role, @Valid @RequestBody Role body) throws Exception {
    Role response = rolesService.replaceRoleForProject(projectId, role, body);
    return new ResponseEntity<>(response, HttpStatus.OK);
  }

  /**
   * PATCH /projects/{project_id}/roles/{role} : Update the specified Role
   *
   * @param projectId (required)
   * @param role (required)
   * @param body (required)
   * @return OK (status code 200)
   *         or Unexpected error (status code default)
   */
  @PatchMapping(
    value = "/projects/{project_id}/roles/{role}",
    produces = { "application/json" },
    consumes = { "application/json" }
  )
  public ResponseEntity<Role> updateRoleForProject(@PathVariable("project_id") Long projectId, @PathVariable("role") Long role, @Valid @RequestBody Role body) throws Exception {
    Role response = rolesService.updateRoleForProject(projectId, role, body);
    return new ResponseEntity<>(response, HttpStatus.OK);
  }

  /**
   * DELETE /projects/{project_id}/roles/{role} : Delete the specified Role
   *
   * @param projectId (required)
   * @param role (required)
   * @return OK (status code 200)
   *         or Unexpected error (status code default)
   */
  @DeleteMapping(
    value = "/projects/{project_id}/roles/{role}"
  )
  public ResponseEntity<Void> deleteRoleForProject(@PathVariable("project_id") Long projectId, @PathVariable("role") Long role) throws Exception {
    rolesService.deleteRoleForProject(projectId, role);
    return new ResponseEntity<>(HttpStatus.OK);
  }

  /**
   * GET /sprints/{sprint_name}/roles : List Roles
   *
   * @param sprintName (required)
   * @return OK (status code 200)
   *         or Unexpected error (status code default)
   */
  @GetMapping(
    value = "/sprints/{sprint_name}/roles",
    produces = { "application/json" }
  )
  public ResponseEntity<RoleList> getRolesForSprint(@PathVariable("sprint_name") Long sprintName, ApiPageable pageable) throws Exception {
    RoleList response = rolesService.getRolesForSprint(sprintName, pageable);
    return new ResponseEntity<>(response, HttpStatus.OK);
  }

  /**
   * POST /sprints/{sprint_name}/roles : Create a Role
   *
   * @param sprintName (required)
   * @param body (required)
   * @return OK (status code 200)
   *         or Unexpected error (status code default)
   */
  @PostMapping(
    value = "/sprints/{sprint_name}/roles",
    produces = { "application/json" },
    consumes = { "application/json" }
  )
  public ResponseEntity<Role> createRoleForSprint(@PathVariable("sprint_name") Long sprintName, @Valid @RequestBody Role body) throws Exception {
    Role response = rolesService.createRoleForSprint(sprintName, body);
    return new ResponseEntity<>(response, HttpStatus.OK);
  }

  /**
   * GET /sprints/{sprint_name}/roles/{role} : Read the specified Role
   *
   * @param sprintName (required)
   * @param role (required)
   * @return OK (status code 200)
   *         or Unexpected error (status code default)
   */
  @GetMapping(
    value = "/sprints/{sprint_name}/roles/{role}",
    produces = { "application/json" }
  )
  public ResponseEntity<Role> getRoleForSprint(@PathVariable("sprint_name") Long sprintName, @PathVariable("role") Long role) throws Exception {
    Role response = rolesService.getRoleForSprint(sprintName, role);
    return new ResponseEntity<>(response, HttpStatus.OK);
  }

  /**
   * PUT /sprints/{sprint_name}/roles/{role} : Replace the specified Role
   *
   * @param sprintName (required)
   * @param role (required)
   * @param body (required)
   * @return OK (status code 200)
   *         or Unexpected error (status code default)
   */
  @PutMapping(
    value = "/sprints/{sprint_name}/roles/{role}",
    produces = { "application/json" },
    consumes = { "application/json" }
  )
  public ResponseEntity<Role> replaceRoleForSprint(@PathVariable("sprint_name") Long sprintName, @PathVariable("role") Long role, @Valid @RequestBody Role body) throws Exception {
    Role response = rolesService.replaceRoleForSprint(sprintName, role, body);
    return new ResponseEntity<>(response, HttpStatus.OK);
  }

  /**
   * PATCH /sprints/{sprint_name}/roles/{role} : Update the specified Role
   *
   * @param sprintName (required)
   * @param role (required)
   * @param body (required)
   * @return OK (status code 200)
   *         or Unexpected error (status code default)
   */
  @PatchMapping(
    value = "/sprints/{sprint_name}/roles/{role}",
    produces = { "application/json" },
    consumes = { "application/json" }
  )
  public ResponseEntity<Role> updateRoleForSprint(@PathVariable("sprint_name") Long sprintName, @PathVariable("role") Long role, @Valid @RequestBody Role body) throws Exception {
    Role response = rolesService.updateRoleForSprint(sprintName, role, body);
    return new ResponseEntity<>(response, HttpStatus.OK);
  }

  /**
   * DELETE /sprints/{sprint_name}/roles/{role} : Delete the specified Role
   *
   * @param sprintName (required)
   * @param role (required)
   * @return OK (status code 200)
   *         or Unexpected error (status code default)
   */
  @DeleteMapping(
    value = "/sprints/{sprint_name}/roles/{role}"
  )
  public ResponseEntity<Void> deleteRoleForSprint(@PathVariable("sprint_name") Long sprintName, @PathVariable("role") Long role) throws Exception {
    rolesService.deleteRoleForSprint(sprintName, role);
    return new ResponseEntity<>(HttpStatus.OK);
  }

}
