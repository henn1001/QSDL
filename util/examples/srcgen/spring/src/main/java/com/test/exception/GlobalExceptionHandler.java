/**
 * NOTE: This class was generated by QSDL.
 */
package com.test.exception;

import java.util.ArrayList;
import java.util.List;
import javax.servlet.http.HttpServletRequest;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.http.HttpHeaders;
import org.springframework.http.HttpStatus;
import org.springframework.http.ResponseEntity;
import org.springframework.validation.FieldError;
import org.springframework.validation.ObjectError;
import org.springframework.web.bind.MethodArgumentNotValidException;
import org.springframework.web.bind.annotation.ControllerAdvice;
import org.springframework.web.bind.annotation.ExceptionHandler;
import org.springframework.web.context.request.ServletWebRequest;
import org.springframework.web.context.request.WebRequest;
import org.springframework.web.servlet.mvc.method.annotation.ResponseEntityExceptionHandler;

import com.test.constants.AppError;
import com.test.model.ApiError;
import com.test.util.Time;

@ControllerAdvice
public class GlobalExceptionHandler extends ResponseEntityExceptionHandler {

  private static Logger log = LoggerFactory.getLogger(GlobalExceptionHandler.class.getSimpleName());

  private ResponseEntity<Object> buildResponseEntity(ApiError error,
      HttpServletRequest httpRequest) {

    error.setPath(httpRequest.getRequestURI());
    error.setTimestamp(Time.now());

    log.warn(error.toString());

    return new ResponseEntity<>(error, HttpStatus.valueOf(error.getStatus()));
  }

  /**
   * Handle our Exceptions.
   */
  @ExceptionHandler({ApiException.class})
  protected ResponseEntity<Object> handleApiException(ApiException ex, WebRequest request,
      HttpServletRequest httpRequest) {

    return buildResponseEntity(ex.getError(), httpRequest);
  }

  /**
   * Fall-back handler.
   * 
   * Catch-all other exceptions that don't have specific handlers.
   */
  @ExceptionHandler({Exception.class})
  public ResponseEntity<Object> handleAll(Exception ex, WebRequest request,
      HttpServletRequest httpRequest) {

    log.error("Caught unhandeled exception:", ex);

    ApiError error = AppError.INTERNAL_SERVER_ERROR
        .getError()
        .addDetailsItem(ex.toString());

    return buildResponseEntity(error, httpRequest);
  }

  /**
   * Handles standard Spring MVC Exceptions.
   */
  @Override
  protected ResponseEntity<Object> handleExceptionInternal(Exception ex, Object body,
      HttpHeaders headers,
      HttpStatus status, WebRequest request) {

    HttpServletRequest httpRequest = ((ServletWebRequest) request).getRequest();

    ApiError error = AppError.BAD_REQEST
        .getError()
        .addDetailsItem(ex.toString());

    return buildResponseEntity(error, httpRequest);
  }

  /**
   * Handles bean validation related Exceptions.
   */
  @Override
  protected ResponseEntity<Object> handleMethodArgumentNotValid(MethodArgumentNotValidException ex,
      HttpHeaders headers,
      HttpStatus status, WebRequest request) {

    HttpServletRequest httpRequest = ((ServletWebRequest) request).getRequest();

    List<String> errors = new ArrayList<String>();
    for (FieldError error : ex.getBindingResult().getFieldErrors()) {
      errors.add(error.getField() + ": " + error.getDefaultMessage());
    }

    for (ObjectError error : ex.getBindingResult().getGlobalErrors()) {
      errors.add(error.getObjectName() + ": " + error.getDefaultMessage());
    }

    ApiError error = AppError.BAD_REQEST
        .getError()
        .setDetails(errors);

    return buildResponseEntity(error, httpRequest);
  }

}
